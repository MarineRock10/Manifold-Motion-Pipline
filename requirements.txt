# Core dependencies for manifold_motion (MuJoCo + frozen SONIC + PPO).
numpy
mujoco
onnxruntime
torch
pyyaml

# Optional: mp4 recording for `python -m manifold_motion.run --video ...`
opencv-python

# Optional: diagnostic figures for `python -m manifold_motion.visualize`
matplotlib
