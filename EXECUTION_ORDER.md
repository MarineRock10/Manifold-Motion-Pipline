# 执行顺序与自我约束

> 用途：**约束我的行为**。阶段未通过验收，不得开始下一阶段。
> 本文档由用户定义目标与顺序；我负责执行，任何偏离必须先改本文档并说明理由。
> 配套：[`MINIMAL_GOAL_AND_PATH.md`](MINIMAL_GOAL_AND_PATH.md)（整条 pipeline 的走通定义）、
> [`STATIC_MANIFOLD_PIPELINE.md`](STATIC_MANIFOLD_PIPELINE.md)（已验证的静态链路）。

---

## 0. 四条硬规矩

1. **顺序不可跳**：Phase 1 → 2 → 3 → 4。上一阶段的验收数字达标，才开工下一阶段。
2. **每个阶段必须落三样**：可复现命令、落盘产物（`.npz` / `.json` / `.pt`）、**量化验收数字**。没有数字 = 没做完。
3. **接口先冻结**（§2）。要改接口 = 停下来说明理由并等确认，不许边做边改。
4. **长任务（>2 分钟）跑完立刻单独汇报**：结论 + 数字 + 产物路径，不等用户问。

## 1. 行为细则（防止我跑偏）

- 不删除、不重写已经验证过的模块；新能力放新文件。
- 任何"更快/更好"的改动，**先给 A/B 对照数字**，再改默认值；没有对照不许动默认。
- 不确定就做 **≤10 分钟能出数字** 的最小验证实验，不做大重构。
- 不擅自 `git commit`、不擅自扩大范围、不擅自加依赖。
- §3 的"已知约束"禁止重复试错；若实测与约束冲突，先记录并汇报，不静默绕过。
- 每完成一阶段，更新本文档的 §6 进度表和 §5 基线数字。
- 汇报统一用 §7 模板。

## 2. 冻结的接口

```python
M_t   = {"center": [x,y,z], "semi": [h,w,d], "quat": [w,x,y,z]}   # 局部流形（椭球，世界系）
z_p   = {"type": "walk" | "stop", "speed": v, "heading": ψ}        # Phase 2 起加入更多 type
R     = {"t": [H], "q": [H,29], "dq": [H,29], "base_pos": [H,3],
         "base_quat": [H,4], "contact": [H,2]}                     # 与 SonicController 参考同构
sample = {"M": M_t, "z_p": z_p, "s": s_t, "H": 历史窗, "c": c_t, "R": R_{t:t+H}}
metrics = {"track_err", "containment_r", "speed", "min_clearance", "fell", "success", "wall_time"}
```

`R` 的字段就是 `ReferenceBuffer`（joint_pos / joint_vel / root_pos / root_quat）的同构体：**"planner 版"与"flow matching 版"对执行层是同一个东西**，所以生成模型可以后换、链路不用改。

### 2.1 两级责任边界（防止把两者的目标混在一起）

架构分解是 `p(R|M,s,c,H) = Σ_zp p_θ(R|M,z_p,s,c,H) · p_φ(z_p|M)`：

| | Stage 1（Phase 2） | Stage 2（Phase 3） |
|---|---|---|
| 学什么 | `p_φ(z_p \| M)`，**原语分布** | `p_θ(R \| M,z_p,s,c,H)`，**运动分布** |
| 输出 | `z_p = [k, α_stance, v]`，低维 | `R` 序列（q, dq, base, contact） |
| 时间尺度 | 流形变了才重选一次 | 每个窗口生成一条 |
| 多模态在哪 | 同一流形下**多种可行意图**（侧身 / 慢走 / 停） | 同一意图下**多种动态展开**（crouch-walk / turn / stop） |
| 监督来源 | RL（可行性，来自能力图）+ 模仿（多风格示范） | 反向采集的动态数据（Phase 1 窗口） |
| 验收 | 可行性 ≥95%，可行区无空洞，不做多余姿态 | 跟踪误差 ≤ planner×1.3，N 候选多样性 |

三条纪律：

1. **Stage 1 的输出必须低维**。它是分类+回归的分布，不是 29 维关节分布；给它加压会退化成"探索噪声"（静态阶段已验证：最优 `p(z_p|M)` 本就是点质量）。
2. **Stage 1 的"最优"由可执行性定义，不由"好看"定义**。可行性边界（净高 1.26 m、半宽 0.35 m）比"哪个更好"更重要。
3. **多风格/多模态是 Stage 2 的指标**（设计文档 §20 实验 8），不进 Phase 2 的门槛。

## 3. 已知约束（实测结论，禁止重复试错）

| 约束 | 实测 | 含义 |
|---|---|---|
| 冻结 SONIC 可执行通道 | 腿（蹲，~1.3 饱和）✔、腰转 ✔；腰前倾 ✘、手臂 ✘ | 动作空间只能建立在这两维上 |
| 蹲行/爬行 | 叠加蹲偏移后 vx 0.03、双脚 0.99/1.00 接触 | 低矮+移动的组合当前接口拿不到 |
| planner 的 `height` 输入 | `-1` 与 `0.75` 输出逐位相同 | 空操作，蹲要靠关节偏移 |
| planner 上下文 | 喂**机器人状态**能走（1.87 m/4 s）；喂**参考自身**会摔 | 必须喂状态 |
| 命令→速度映射 | `target_vel 0.4`→站住；`1.0`/`-1`→0.49 m/s | 不是 m/s，用前先扫描标定 |
| 身体模型标定协议 | `jump` 协议导致系统乐观（0.87 vs 1.27） | 必须 `--protocol slew` |
| pelvis landmark | 用网格极点会让骨盆相对化整体错位 0.148 m | pelvis 必须用原点 |
| 可行性判定 | 用 `min` over landmarks 会把不可行当可行 | 必须 `max`（最差 landmark） |
| 奖励设计 | 弱越界惩罚→拒绝摆姿态；余量奖励不封顶→全部抱团 | 越界势垒陡、余量奖励封顶 |
| 训练性能 | 32 并行流形时 29 µs/env-step；600 轮 ≈ 5 分钟 | 算力不是瓶颈 |
| ORT 线程 | 默认 intra=核数、inter=核数；6 worker × 3 会话会让 load 冲到 76 | 会话设 intra=2、inter=1 |
| planner 调用频率 | replan 每 5/10/20 tick → 1.32×/1.82×/2.38× 实时；跟踪 p50 0.039/—/0.058 rad | 取 `--replan-every 10`（5 Hz）：速度与质量折中 |
| viewer 键位 | `[` `]` 被 MuJoCo 占作切相机 | 用 `u/i j/k ,/. n/m t/y` |

## 4. 阶段划分

### Phase 1：数据（随机轨迹 → SONIC 执行 → 反向动态映射数据）

**步骤**

1.1 **命令标定扫描**：planner 的 `target_vel` / `movement` / `facing` → 实际速度、朝向、包络。产出"命令 → 实际"表（也是 `M^R` 的第一块数据）。
1.2 **随机轨迹生成器**：分段随机命令（每段 2–5 s，含转向），保证在有限区域内不越界；固定随机种子可复现。
1.3 **并行采集器**：K 个进程同时跑 episode，每条落 `.npz`（逐 tick：命令/实际关节、base、接触、包络）+ `.json`（汇总）。
1.4 **反向提取**：逐 tick 生成样本 `(M_t, z_p_task, s_t, H_t, c_t) → R_{t:t+H}`，滑窗 H = 1 s、stride 5，按 **episode** 划分 train/val（不按窗口，避免泄漏）。
1.5 **数据体检**：覆盖率、包络分布、合格率、跟踪误差分布，出图/出表。

**交付物**：`reports/manifold_g1/clips/*.npz|json`；`reports/manifold_g1/dataset/`；上述两张表。

**验收门槛**（可改，先按此执行）

- 规模：≥ 100 episodes、≥ 30 分钟仿真时长
- 质量：合格率（不摔 且 跟踪误差均值 < 0.06 rad）≥ 90%
- 数据集：窗口数 ≥ 15k（30 分钟仿真 = 90k tick，stride 5 → 18k 窗口；若真要 200k 窗口，需 ≈5.5 小时仿真 ≈ 6 进程 1 小时）
- 覆盖：命令网格（mode × 速度 × 朝向）覆盖率 ≥ 80%；包络 (h,w,d) 每维极差 ≥ 0.10 m
- 成本：并行 6 进程，总耗时 ≤ 15 分钟

**明确不做**：多原语（只有 walk/stop）、避障、真实场景随机化。

---

### Phase 2：原语模型（RL + 动态数据）

**步骤**

2.0 **能力图**（先做，否则 2.1 不成立）：Phase 1 数据里没有 `M → z_p` 的监督——流形是从动作**反推**的，直接克隆 `M,s,c → c` 是"用果推因"。正确做法是把数据当能力探针：对一族走廊（净高 × 半宽）做**持续可行性**判定（1 秒滑窗内每一 tick 都必须装得下，避免"某一瞬间恰好够低"的统计偏差），得到"这条走廊能不能走、最快多少"。产出 `dataset/capability.json`。

2.1 用能力图做监督初始化（`M,s → v_max`，必要时再行为克隆 `M,s,c → z_p`）。
2.2 几何环境 RL 微调：奖励 = **可执行性**（来自 Phase 1 的可行/不可行证据）+ 任务 + 经济 + 稳定。
2.3 与"查表/规则"基线对照。

**交付物**：`primitive.pt`、`p(z_p|M)` 的评估表、与基线的对照表。

**验收门槛**

- 留出流形上，`z_p` 在 SONIC 上执行成功率 ≥ 95%
- 与数据中实际最优命令的速度偏差 ≤ 0.15 m/s
- 训练耗时 ≤ 10 分钟；推理 ≤ 5 ms

**明确不做**：二阶模仿（无示范数据）、多模态原语。

---

### Phase 3：动态模型（Conditional Flow Matching）

**步骤**

3.1 数据集与条件编码器（M, z_p, s, H, c）。
3.2 训练 flow matching，输出 `R`（先直接预测 q/dq/base/contact）。
3.3 多候选采样（N 条）+ `J(R)` 排序 → R*。
3.4 与 planner 基线对照（跟踪误差、可行性、多样性、耗时）。

**交付物**：`flow.pt`、采样脚本、对照表。

**验收门槛**

- 跟踪误差 ≤ planner 基线 × 1.3
- 可行性率（SONIC 执行不摔且跟踪 < 0.06 rad）≥ 95%
- 多样性：N=8 候选两两平均距离 ≥ 0.05 rad
- 单条推理 ≤ 100 ms（CPU）

**明确不做**：二阶模仿、latent 压缩、多原语条件。

---

### Phase 4：目标点 → 直线 → 流形 → 最优动作分布 → SONIC（先不做避障）

**步骤**

4.1 目标点 → 世界系直线路径。
4.2 直线 → 流形序列：沿路径排椭球，半径 = 走路包络 + 余量（来自 Phase 1 实测）。
4.3 `M + s` → 原语（Phase 2）→ 动态模型（Phase 3）→ N 候选 → `J(R)` → R*。
4.4 SONIC 执行，逐 tick 判定包含度与间距，汇总指标。

**交付物**：`run_goal.py`、端到端 episode 报告、3 个目标点的成败表。

**验收门槛**

- 目标点（前 5 m / 侧 3 m / 后 3 m）各 ≥ 2/3 到达
- 全程最小间距 > 0；无摔
- 单 episode 的"规划+生成"耗时 ≤ 2 s

**明确不做**：点云/SDF/A*/TrajOpt、动态障碍、真实机器人。

---

## 4.5 能力图实测（Phase 2.0 产出，阶段 4 的流形半径依据）

持续可行（1 秒窗口内所有 tick 都装得下）的行走速度：

| 净高 \ 半宽 | 0.35 | 0.41 | 0.46 | ≥0.52 |
|---|---|---|---|---|
| 1.20 / 1.23 m | ✗ | ✗ | ✗ | ✗ |
| 1.26 m | ✗ | 0.63 | 0.67 | 0.69 m/s |
| 1.29 m | 0.80 | 0.77 | 0.80 | 0.80 m/s |
| ≥1.32 m | 0.81 | 0.78 | 0.78 | 0.78 m/s |

- **净高 1.26 m 以下走不了**（头顶 p5–p95 = 1.254–1.307 m，所以 1.29 m 起才基本全程可走）；
- 半宽 0.35 m（0.70 m 宽走廊）在 1.29 m 净高下仍有 435 个可行窗口 → **侧移/转向能把身体收窄通过**；
- 数据里行走速度 p5/p50/p95 = 0.28/0.59/0.90 m/s。

## 5. 基线数字（对照用，勿改）

| 指标 | 当前值 | 来源 |
|---|---|---|
| 正常走路跟踪误差 | 0.033 rad | `clips/walk.json` |
| 走路 4 s 前进 | 1.87 m（0.49 m/s） | 同上 |
| 静态姿态真机验证差 | 0.01–0.05 | `bc/verify_sonic.json` |
| 蹲行 | 不可执行（vx 0.03） | `clips/crouchwalk08.json` |
| 静态策略训练 | 600 轮 / ~5 分钟 | `bc/train_log.csv`（历史，见 `sonic_rl/train_log.csv`） |
| 录制一个 4 s 片段 | 3.4 s 挂钟（1.2× 实时，单进程） | `clips/*.json` |
| 并行采集吞吐 | 2.05× 实时（6 进程，`--replan-every 10`） | `clips/batch_seed1000.json` |
| 随机走路跟踪误差 | p50 0.048 rad / max 0.056 rad | `clips/ep*.json` |
| 数据集体量 | 18705 窗口 / 105 episodes / 2028 s 仿真 | `dataset/health.json` |
| 执行层诊断 | 16 s | `pose_tracking.json` |

## 6. 进度表

| 阶段 | 状态 | 验收数字 | 产物 |
|---|---|---|---|
| Phase 1 数据 | ✅ 完成 | 100 episodes / 2028 s 仿真 / 18705 窗口 / 合格率 1.00 | `clips/`、`dataset/index.json`、`dataset/health.json`、`clips/command_table.json` |
| Phase 2.0 能力图 | ✅ 完成 | 详见下方"能力图实测" | `dataset/capability.json` |
| Phase 2 原语模型 | 🟡 进行中 | — | — |
| Phase 3 动态模型 | ⬜ 未开始 | — | — |
| Phase 4 目标点→动作 | ⬜ 未开始 | — | — |

## 7. 汇报模板（我每次汇报按这个）

1. **结论**（一句话：做完了什么 / 卡在哪）
2. **数字**（表格：指标 | 本次 | 验收门槛 | 结论）
3. **产物路径**（可点开）
4. **与门槛的差距**（不达标就直说，不修饰）
5. **下一步 + 风险**
