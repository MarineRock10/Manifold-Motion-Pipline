# 总体架构

> 本文是**唯一**的架构文档：系统要达到什么、分几层、层与层之间的接口是什么、哪些是冻结的。
> 配套：[`PIPELINE.md`](PIPELINE.md)（当前实现的技术路线与实测数字）。
> 取代了 `G1_Manifold_Hierarchical_Motion_Generation_Technical_Design(1).md`、
> `Robot_aware_Manifold_to_Motion_Project_Design_v4.md`、`MINIMAL_GOAL_AND_PATH.md`、
> `EXECUTION_ORDER.md`（历史版本见 git）。
>
> 日期：2026-09-16。

---

## 1. 目标

让 Unitree G1 在**给定时变椭球流形** `M_t` 和任务条件（速度、朝向、身高）下，生成**物理可行、且始终待在流形内**的运动，执行层是**冻结的** SONIC 全身体控制器（ONNX，不解冻、不微调）。

流形不是障碍物，是**软几何约束**：它描述"身体表面应该待在哪个椭球里"。这让"钻进矮通道""侧身穿窄缝"这类任务变成一个连续几何问题，而不是离散的碰撞避免。

**核心难点不是生成动作，是生成执行层真的能做到的动作。** 这一条决定了整个架构：训练信号必须来自真控制器实测的落点，而不是对控制器的建模。

---

## 2. 分层架构

```
环境                          Stage 0              Stage 1            Stage 2                执行
─────                        ────────             ────────           ────────              ────
感知 / 地图                   M            →        z_p         →      R (T 帧运动)    →     SONIC
                            (椭球流形)           (运动原语)          (关节轨迹)            G1 物理
                                                    ↑                    ↑
                                              Robot Capability      State / History
                                                Manifold
```

| Stage | 输入 | 输出 | 职责 | 当前状态 |
|---|---|---|---|---|
| **0 流形生成** | 地图 / 走廊 / 任务 | `M` 椭球 | 把环境约束变成几何 | 静态手写（本文档范围内） |
| **1 原语选择** | `M`, 任务条件 `c` | `z_p` 原语 + 姿态 | 决定"摆成什么姿态 / 用什么步态" | **已训**（姿态部分，见 PIPELINE） |
| **2 动态生成** | `M`, `z_p`, 状态历史 | `R`：T 帧关节轨迹 | 决定"怎么动起来" | 未开始 |
| **执行** | `R` | 实测关节状态 | 冻结 SONIC + MuJoCo | 已接 |

**分层原则**：Stage 1 输出**一个**驻留姿态（"摆成什么姿态"），它的**时间展开是 Stage 2 的工作**。两者分开，是因为"姿态对不对"和"动起来跟不跟得上"是两个不同的可行性问题，混在一起训练会互相掩盖。

---

## 3. 冻结的接口

改接口必须明确说明理由并等确认——接口一改，上下游全部作废。这四条是硬约束。

### 3.1 流形 `M`

```python
M = {"center": [x,y,z], "semi": [sx,sy,sz], "quat": [w,x,y,z]}
```

- 椭球，**骨盆坐标系**，`center` 是身体相对骨盆的位置（不是骨盆本身：它在骨盆下方约 0.11 m，因为身体大部分是腿）；
- `semi` 是**拟合**椭球的半轴，不是各轴最大偏移。两者是不同的形状：用盒子的半轴当椭球半轴，站姿在**自己的** scale-1.0 流形下算出 r = 1.145，"装进流形"就变成不可满足的目标（详见 PIPELINE §3）；
- 包含性判据：`r = ||(p − center)/semi||` 对所有身体表面点取最大值；`r ≤ 1` 表示在内部。**这是全项目唯一的判据。**

### 3.2 原语 `z_p` 与姿态 `q`

- `q`：29 维关节向量，**policy(IsaacLab) 顺序**，是相对默认站姿的**增量**；
- 观测：15 维，单一定义在 `pose_policy.observation`：
  `pose[:3]/1.4 (3) + mean,std (2) + r (1) + (center−pelvis)/0.5 (3) + semi (3) + axis (3)`；
- 动作映射：`q = tanh(a) * 1.4`，单一定义在 `pose_policy.action_to_pose`。用 `tanh` 不用 `clamp`——clamp 会饱和并失去"回来"的梯度。

### 3.3 运动 `R`

```python
R = {"joint_pos": [T,29], "joint_vel": [T,29],
     "root_pos": [T,3], "root_quat": [T,4]}          # 全部 policy 顺序
```

SONIC 读的是**运动参考**：逐帧的关节位置/速度和根位姿。一个**关键帧就是 T=1 的 R**——这是静态姿态能直接接到 SONIC 上的原因（`reference.KeyframeReference`）。

### 3.4 执行层

- SONIC ONNX，50 Hz 控制 / 200 Hz 物理（4 子步）；
- `q_target = default_angles[isaaclab_to_mujoco] + action * g1_action_scale`，PD 力矩控制；
- **唯一可信的"到达姿态"是 MuJoCo 里的实测值**，不是任何模型的预测。

---

## 4. 当前范围与不在范围内的东西

**范围内**：Stage 1 的静态姿态部分（`M → q`），以及它的执行与验证。

**不在范围内（但接口已按它们的需要冻结）**：

| 不在范围 | 为什么接口仍然按它定 |
|---|---|
| 环境感知 / 全局路径 / Safe Corridor → `M_t` | `M` 的格式必须能表达时变流形，否则 Stage 1 训完就得推翻 |
| Stage 2 的 flow matching / N 候选 / J(R) 选择 | `z_p` 和 `R` 分开，就是为了让 Stage 2 能独立升级 |
| 动态流形反向提取（从运动反推 `M_t`） | 它产出 `M`，用的是同一条 `r` 判据 |

### 4.1 Stage 1 为什么需要模仿项

只靠 RL 学 `M → q` 会崩：奖励稀疏（整身进椭球并保持住才算成功）、动作 29 维、流形池大部分很难随机探索到。先克隆示范（`M, q` 对）把策略放进姿态空间的正确区域，RL 再在区域内精修。

**但"区域内"的判据必须是真控制器，不是模型。** 早期尝试学一个残差模型 `Δ̂(q)` 预测 SONIC 会做什么，再用它做 RL——那个偏差（优于忽略残差 51%）不等于预测正确，目标变成了"我以为控制器会做什么"。现在真控制器直接进环：一个姿态约 0.3 s 物理，全量微调 879 s，完全可接受。这条路线已删除（见 PIPELINE §4）。

---

## 5. 阶段与验收

每个阶段落三样：**可复现命令、落盘产物、量化验收数字**。没有数字等于没做完。

| Gate | 流形 | 能力 | 状态 |
|---|---|---|---|
| L0 | 无 | 关键帧 → 冻结 SONIC 能站住/保持 | ✅ |
| L1 | 静态椭球 | 姿态策略把身体装进流形，模型与真机一致 | ✅ 后被在环路线取代 |
| L2 | 记录的包络家族 | 克隆 `(M, s) → q` | ✅ 拟合 94.9% |
| L3 | 扰动的包络 | 在真控制器落点上微调 | ✅ success 1.00, r 0.82 |
| L4 | 全部 | **度量口径正确**（椭球、MARGIN 一致） | ✅ |
| L5 | 全部 | 超出当前姿态通道的流形（倾斜/极窄） | ❌ **卡在这里**，见 PIPELINE §6 |
| L6 | 随机 p(M) | 多模态 π(M, s, c, z) | 未开始 |

### 5.1 硬规矩

1. **顺序不可跳**：上一阶段验收数字达标才开工下一阶段；
2. **每个阶段必须有量化验收数字**；
3. **接口先冻结**，要改先说明理由；
4. **任何"更快/更好"的改动先给 A/B 对照数字**，再改默认值，没有对照不许动默认；
5. **不确定就做 ≤10 分钟能出数字的最小实验**，不做大重构；
6. **长任务（>2 分钟）跑完立刻汇报**：结论 + 数字 + 产物路径。

---

## 6. 四个必须记住的约束（重复试错代价最高）

### 6.1 判据是椭球，不是盒子
`r = ||(p−c)/semi||`。盒子的半轴当椭球半轴用，角点会到 r = 1.73。项目里所有 `r` 都必须出自这一条判据，写入端也必须存椭球。

### 6.2 训练分布与评估分布必须同尺
示范集的流形带 `MARGIN = 1.05`，评估的 scale 1.0 不带——差 5%，实测吻合到三位小数（预测 1.029，实测 1.030）。`family.MARGIN` 现在是唯一定义。

### 6.3 "包络内性"和"控制器跟不跟得上"必须一起看
加大倾斜扰动让策略学会用 **waist pitch** 凑姿态，而 waist pitch 正是冻结控制器基本不跟的通道——结果每一行都变坏（median 0.975 → 1.200），waist 跟踪误差从 0.03 跳到 0.28 rad。**一个姿态通道如果控制器不认，训练压力再大也没用。**

### 6.4 控制器有它不认的维度
实测（扫通道后把到达关节拟合回方向）：

| 通道 | 跟不跟 |
|---|---|
| crouch（髋/膝/踝） | 跟，但饱和：要 1.6–2.0 只给到 ≈1.2 |
| twist（腰 yaw + 肩 yaw） | 跟 |
| lean（腰 pitch） | **基本不跟** |
| arms（肩 roll、肘） | 深姿态下**不跟**：手臂关键帧不被采纳 |

这直接限定了任何策略能要求什么。要摆手臂必须换通道（SONIC release 支持 `vr_3point_local_target` / `vr_3point_local_orn_target`，显式手/头目标）。这是 L5 卡住的**根本原因**，也是下一步的方向。
